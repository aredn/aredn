--- a/feeds/morse/essentials/morse_driver/patches/018-CVE-security-fix.patch
+++ /dev/null
@@ -1,31 +0,0 @@
-commit 32aee324965f492fc9c4a9c2ec2b6b85b82a53a6
-Author: Ayman Grais <ayman.grais@morsemicro.com>
-Date:   Wed Apr 1 21:44:03 2026 +0000
-
-    SW-19664 Out of bounds vendor IE length validation (pull request #2074)
-    
-    * Add a length check in morse_vendor_find_vendor_ie so it
-    only returns an IE that is large enough to be safely dereferenced
-    
-    The callers will get NULL if nothing found matching the
-    length + the OUI. Caller is checking for NULL anyway.
-    
-    
-    Approved-by: Simon Wadsworth
-    Approved-by: Dan Callaghan
-    Approved-by: Pradeep Reddy
-
-diff --git a/vendor.c b/vendor.c
-index d1adb2e40..3fe87136c 100644
---- a/vendor.c
-+++ b/vendor.c
-@@ -311,7 +311,8 @@ morse_vendor_find_vendor_ie(struct dot11ah_ies_mask *ies_mask)
- 
- 	while (cur && cur->ptr) {
- 		ie = (struct dot11_morse_vendor_caps_ops_ie *)cur->ptr;
--		if (memcmp(ie->oui, morse_oui, sizeof(ie->oui)) == 0 &&
-+		if (cur->len >= sizeof(*ie) &&
-+		    memcmp(ie->oui, morse_oui, sizeof(ie->oui)) == 0 &&
- 		    ie->oui_type == MORSE_VENDOR_IE_CAPS_OPS_OUI_TYPE) {
- 			found = true;
- 			break;
--- a/feeds/morse/essentials/morse_driver/patches/017-apsta-channel-switch.patch
+++ /dev/null
@@ -1,61 +0,0 @@
-commit 4d0a73201302bb17065423e5a2a87582d004bbdb
-Author: Prabha Murugesan <prabha.murugesan@morsemicro.com>
-Date:   Wed Sep 17 17:33:53 2025 +1000
-
-    APP-4271 : apsta mode channel alignment bug fixes.
-
-diff --git a/mac.c b/mac.c
-index d0222538..a718dcf3 100644
---- a/mac.c
-+++ b/mac.c
-@@ -646,6 +646,23 @@ static struct ieee80211_vif *morse_get_first_vif_of_type(struct morse *mors,
- 	return vif;
- }
- 
-+struct ieee80211_vif *morse_get_active_ap(struct morse *mors)
-+{
-+	int vif_id = 0;
-+	struct ieee80211_vif *vif = NULL;
-+
-+	spin_lock_bh(&mors->vif_list_lock);
-+	for (vif_id = 0; vif_id < mors->max_vifs; vif_id++) {
-+		vif = __morse_get_vif_from_vif_id(mors, vif_id);
-+		if (vif && vif->type == NL80211_IFTYPE_AP && vif->bss_conf.enable_beacon)
-+			break;
-+
-+		vif = NULL;
-+	}
-+	spin_unlock_bh(&mors->vif_list_lock);
-+	return vif;
-+}
-+
- struct ieee80211_vif *morse_get_ap_vif(struct morse *mors)
- {
- 	return morse_get_first_vif_of_type(mors, NL80211_IFTYPE_AP);
-@@ -3321,6 +3338,8 @@ static int morse_mac_change_channel(struct ieee80211_hw *hw)
- 	const char *region = morse_dot11ah_get_region_str();
- 	int vif_id;
- 	bool have_ap = morse_get_ap_vif(mors) ? true : false;
-+	bool have_sta = morse_get_sta_vif(mors) ? true : false;
-+	bool have_active_ap = morse_get_active_ap(mors) ? true : false;
- 
- 	/* Convert 5G channel to S1G channel */
- 	chan_s1g = morse_dot11ah_channel_chandef_to_s1g(&conf->chandef);
-@@ -3339,7 +3358,7 @@ static int morse_mac_change_channel(struct ieee80211_hw *hw)
- 		(chan_s1g->ch.flags & IEEE80211_CHAN_2MHZ) ? 2 : 1;
- 
- 	/* Update primary channel info based on BSS only if no AP interfaces */
--	if (!have_ap) {
-+	if (!have_ap || ( have_sta && !have_active_ap) ) {
- 		for (vif_id = 0; vif_id < mors->max_vifs; vif_id++) {
- 			struct ieee80211_vif *vif = morse_get_vif_from_vif_id(mors, vif_id);
- 
-@@ -3352,7 +3371,7 @@ static int morse_mac_change_channel(struct ieee80211_hw *hw)
- 				 * width to 2MHz to be able to receive these responses.
- 				 */
- 				pri_bw_mhz = op_bw_mhz > 1 ? 2 : 1;
--			} else if (vif && vif->bss_conf.bssid) {
-+			} else if (vif && vif->bss_conf.bssid && vif->type != NL80211_IFTYPE_AP) {
- 				/* If we are a STA and have a BSS/AP conf, try to use the AP's chan
- 				 * info.
- 				 */
